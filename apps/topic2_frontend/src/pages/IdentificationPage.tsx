/** 参数辨识页面（旧路由兼容）：渲染 IdentificationWorkspace。 */

import { IdentificationWorkspace } from '../components/learning/IdentificationWorkspace'

export function IdentificationPage() {
  return (
    <div>
      <h1>参数辨识</h1>
      <IdentificationWorkspace />
    </div>
  )
}
