/** 工艺建模页面（旧路由兼容）：渲染 ModelingWorkspace。 */

import { ModelingWorkspace } from '../components/learning/ModelingWorkspace'

export function ModelingPage() {
  return (
    <div>
      <h1>工艺建模</h1>
      <ModelingWorkspace />
    </div>
  )
}
